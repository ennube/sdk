"use strict";
/*

describe('basic tests', ( ) => {

    let exo = new Exoskeleton();

    it('initialization', ( ) => {
        expect( exo.length ).toBe( 0 );
    });

    it('undefined -> array', ( ) => {
        exo.updateValue([1,2,3]);
        expect( exo.length ).toBe( 3 );
    });
    it('array -> array grown', ( ) => {
        exo.updateValue([1,2,3,4,5]);
        expect( exo.length ).toBe( 5 );
    });
    it('array -> array decrease', ( ) => {
        exo.updateValue([1,2]);
        expect( exo.length ).toBe( 2 );
    });


});



describe('exoskeleton basics', ( ) => {

    @Design.class()
    class MyClass {
        @Design.member( Number )
        value: number = 88;
    };

    let exoskeleton;
    let instance = new MyClass();

    it('construction', ( ) => {
        exoskeleton = new Exoskeleton(instance);
        expect( exoskeleton.child('value') ).toBeDefined();
    });
});
*/
//# sourceMappingURL=exoskeleton.spec.js.map